{
    "name": "@prj--personal-portfolio-v2/ds-lsg-styles",
    "version": "0.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "lint": "stylelint ./**/*.scss --config stylelint.config.js",
        "develop": "export NODE_ENV=development && nodemon --watch src --exec yarn run build:dev -e scss",
        "develop:playgrounds": "export NODE_ENV=development && nodemon --watch src --exec yarn run build:dev -e scss",
        "storybook": "export NODE_ENV=development && nodemon --watch src --exec yarn run build:dev -e scss",
        "build:dev": "export NODE_ENV=development && node build/scss.js",
        "build:prod": "rm -rfv ./lib && export NODE_ENV=production && node build/scss.js"
    },
    "devDependencies": {
        "nodemon": "^3.0.1",
        "sass": "^1.69.0",
        "stylelint": "^15.10.3",
        "stylelint-config-sass-guidelines": "^10.0.0"
    },
    "publishConfig": {
        "access": "public"
    },
    "files": [
        "lib",
        "src"
    ]
}
